# Components

Shared UI components for the DevKit UI builder interface.

## Structure

- `ui/` — Base UI primitives (buttons, inputs, etc.)
- `layout/` — Layout components (topbar, sidebar, panels)
- `builder/` — Builder-specific components
